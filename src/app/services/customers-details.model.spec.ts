import { CustomersDetails } from './customers-details.model';

describe('CustomersDetails', () => {
  it('should create an instance', () => {
    expect(new CustomersDetails()).toBeTruthy();
  });
});
